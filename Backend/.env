# Database connection details
DB_NAME=pukkeconnect_schema_only
DB_USER=postgres
DB_PASSWORD=1234
DB_HOST=localhost
DB_PORT=5432

# JWT secret for signing tokens
JWT_SECRET=secretkey_123

# Session secret for encrypting session cookies
SESSION_SECRET=session_secret

# Server port
PORT=4000
